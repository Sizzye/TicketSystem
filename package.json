{
  "name": "repair-desk-web",
  "version": "0.1.0",
  "private": true,
  "description": "Starter web app for an electronics repair shop intake and ticket system.",
  "scripts": {
    "clean": "node -e \"require('fs').rmSync('.next',{ recursive: true, force: true })\"",
    "dev:reset": "npm run clean && next dev",
    "dev": "next dev",
    "build": "next build",
    "start": "next start"
  },
  "dependencies": {
    "next": "^15.0.0",
    "react": "^19.0.0",
    "react-dom": "^19.0.0"
  },
  "devDependencies": {
    "playwright": "^1.59.1"
  }
}
